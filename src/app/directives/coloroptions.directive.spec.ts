   import { ColorOptionsDirective } from './coloroptions.directive';

describe('ColoroptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
